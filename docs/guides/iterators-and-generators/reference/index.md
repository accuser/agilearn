---
title: Reference
---

# Reference: Iterators and generators

Quick-lookup pages. Short, dense, designed to skim. If you're here to learn the topic for the first time, start with [Learn](../learn/) instead.

## Pages in this section

- **[`itertools` cheatsheet](itertools-cheatsheet.md)** — the whole `itertools` module on one page: `chain`, `groupby`, `islice`, `accumulate`, `takewhile`, `dropwhile`, `tee`, and the combinatoric helpers.
- **[Iterator protocol reference](iterator-protocol-reference.md)** — `__iter__`, `__next__`, `StopIteration`, and the distinction between an iterable and an iterator.
- **[Generator syntax reference](generator-syntax-reference.md)** — `yield`, `yield from`, `.send()`, `.close()`, and the generator-expression forms.
